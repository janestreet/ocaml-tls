(library
 ((name tls_async)
  (modules (tls_async))
  (libraries (nocrypto.async core async rresult tls))))

(library
 ((name x509_async)
  (modules (x509_async))
  (libraries (ptime.clock.os core async fpath rresult fmt nocrypto x509))))
