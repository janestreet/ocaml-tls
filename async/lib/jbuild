(library
 ((name tls_async)
  (modules (tls_async))
  (libraries (nocrypto.async core async tls))))

(library
 ((name x509_async)
  (modules (x509_async))
  (libraries (ptime.clock core async fpath fmt nocrypto x509))))
